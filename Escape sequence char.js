let a = 'don\'t';  //if use quote in quote the we use backslash. hey cannot count also.
console.log(a);
console.log(a.length);  // output is 5 not 6.

let b = 'don\'t \ndo';  // do is print in a new line.
console.log(b);
console.log(b.length)